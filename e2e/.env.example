# E2E environment — copy to .env and fill in values
# .env is gitignored and never committed.

# ── Target URL ───────────────────────────────────────────────────────────────
# Local dev server (recommended): Playwright will start Next.js automatically.
# Make sure .env.local exists in the project root with Supabase/TMDB/Groq keys.
BASE_URL=http://localhost:3000

# Remote deployment (CI / manual — requires IP to be whitelisted):
# BASE_URL=https://www.episode.watch

FALLBACK_URL=https://last-episode.vercel.app

# ── Test user ─────────────────────────────────────────────────────────────────
# Must be a real account in Supabase (create once, never delete)
TEST_USER_EMAIL=your@email.com
TEST_USER_PASSWORD=yourpassword

# ── Browser ───────────────────────────────────────────────────────────────────
HEADLESS=true
